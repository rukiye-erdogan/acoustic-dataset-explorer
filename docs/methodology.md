# Methodology

The project demonstrates a reproducible workflow for acoustic metadata.

1. Load structured CSV metadata.
2. Validate the schema and required fields.
3. Check identifiers and numeric values.
4. Calculate dataset statistics.
5. Test the validation workflow.

Only synthetic example metadata is included.
